# Evidências - Configuração de Modelos

Esta pasta deve conter registros visuais e arquivos de configuração que provam o setup operacional do projeto:
- Screenshots do painel do **LM Studio** com os modelos carregados.
- Parâmetros configurados no LM Studio (Context Length, GPU Offload, Temperature).
- Cópias de arquivos de configuração locais (como `.env` ou configs JSON de conexão do AutoGen).
